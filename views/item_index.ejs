<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <% include partials/header.ejs %>
    </head>
    <body>
        <nav class='container'>
            <% include partials/nav.ejs %>
        </nav>
        <div class='cPostBox'>
        <% if(currentUser){ %>
            <a class='cPost' href='/view/new'>Create Post</a>
        <% } %>
        </div>
        <div class='gamecontainer'>
        <h1> Name Games </h1>
            <div class="row">
                <% for( let i = 0; i< posts.length; i++) { %>
                    <a class='col-sm-3 showlink' href='/view/index/<%=posts[i].id%>'><%=posts[i].name %></a>

                <% } %>
            </div>
        </div>
        <div class='gamecontainer'>
        <h1> Git to Know You's </h1>
            <div class="row">
                <% for( let i = 0; i< posts.length; i++) { %>
                    <a class='col-sm-3 showlink' href='/view/index/<%=posts[i].id%>'><%=posts[i].name %></a>

                <% } %>
            </div>
        </div>
        <div class='gamecontainer'>
        <h1> Initiatives </h1>
            <div class="row">
                <% for( let i = 0; i< posts.length; i++) { %>
                    <a class='col-sm-3 showlink' href='/view/index/<%=posts[i].id%>'><%=posts[i].name %></a>

                <% } %>
            </div>
        </div>
        <div class='gamecontainer'>
        <h1> Trust exercises </h1>
            <div class="row">
                <% for( let i = 0; i< posts.length; i++) { %>
                    <a class='col-sm-3 showlink' href='/view/index/<%=posts[i].id%>'><%=posts[i].name %></a>

                <% } %>
            </div>
        </div>

    </body>
</html>
